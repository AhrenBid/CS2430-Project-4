Run instructions:

copy the java files in src into a package named "src" in your ide.
create a resources folder inside the root folder of your project
run main
see console output
see created csvs inside resources
